services:
  app:
    image: golang:1.22
    working_dir: /app
    volumes:
      - .:/app
    command: go run ./cmd/api
    ports:
      - "8080:8080"
    environment:
      APP_NAME: Go Invoicing
      PORT: 8080
